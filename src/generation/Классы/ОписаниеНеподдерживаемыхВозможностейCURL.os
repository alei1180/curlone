Перем _Последствия; // Соответствие из Строка в Строка

#Область ОбработчикиСобытий

Процедура ПриСозданииОбъекта()

	_Последствия = Новый Соответствие;
	ЗаполнитьПоследствия();

КонецПроцедуры

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Объясняет пользователю последствие отсутствующей возможности генератора.
//
// Параметры:
//  Идентификатор - Строка - Код возможности.
//  Условное - Булево - Признак зависимости от вычисления значения или чтения источника.
//
// Возвращаемое значение:
//  Строка - Описание возможной потери без внутренних кодов.
Функция Предупреждение(Идентификатор, Условное) Экспорт

	Если Идентификатор = "headers.repeated" Тогда
		Если Условное Тогда
			Возврат "После чтения источника могут обнаружиться повторяющиеся заголовки. "
				+ "Генератор сохранит только последнее значение.";
		КонецЕсли;

		Возврат "Повторяющиеся заголовки не сохраняются. Останется только последнее значение.";
	КонецЕсли;

	Последствие = _Последствия.Получить(Идентификатор);
	Если Последствие = Неопределено Тогда
		Последствие = "Часть команды не поддерживается и может быть потеряна.";
	КонецЕсли;

	Если Условное Тогда
		Возврат "Ограничение может проявиться после вычисления значения или чтения источника. " + Последствие;
	КонецЕсли;

	Возврат Последствие;

КонецФункции

// Объясняет причину невозможности преобразования команды.
//
// Параметры:
//  Идентификатор - Строка - Код обязательной возможности.
//
// Возвращаемое значение:
//  Строка - Описание причины без внутренних кодов.
Функция Ошибка(Идентификатор) Экспорт

	Последствие = _Последствия.Получить(Идентификатор);
	Если Последствие = Неопределено Тогда
		Возврат "Команду невозможно представить средствами выбранного генератора.";
	КонецЕсли;

	Возврат "Команду невозможно преобразовать. " + Последствие;

КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ЗаполнитьПоследствия()

	Добавить("protocol.http", "Генератор не поддерживает протокол HTTP.");
	Добавить("protocol.https", "Генератор не поддерживает протокол HTTPS.");
	Добавить("protocol.ftp", "Генератор не поддерживает протокол FTP.");
	Добавить("protocol.ftps", "Генератор не поддерживает протокол FTPS.");
	Добавить("protocol.dynamic", "Протокол адреса станет известен только после вычисления значения.");
	Добавить("operations.multiple", "Генератор не поддерживает несколько запросов в одной команде.");
	Добавить("request.explicit_method", "Явно заданный метод запроса может быть заменён методом по умолчанию.");
	Добавить("request.metadata_only", "Запрос только метаданных будет преобразован в обычный запрос.");
	Добавить("request.names_only", "Ответ не будет ограничен именами ресурсов.");
	Добавить("query.parts", "Параметры адреса могут быть объединены с потерей исходной структуры.");
	Добавить("headers.ordered", "Порядок заголовков может измениться.");
	Добавить("headers.empty", "Заголовки с пустым значением могут быть пропущены.");
	Добавить("headers.remove", "Удаление ранее заданных заголовков не будет выполнено.");
	Добавить("headers.file", "Заголовки из файла не будут добавлены в запрос.");
	Добавить("headers.stdin", "Заголовки из стандартного ввода не будут добавлены в запрос.");
	Добавить("headers.json_defaults", "Заголовок для данных JSON не будет установлен автоматически.");
	Добавить("body.data", "Данные тела запроса могут быть перенесены не полностью.");
	Добавить("body.multipart", "Поля и файлы составного тела запроса могут быть потеряны.");
	Добавить("body.upload", "Загружаемый файл не будет передан.");
	Добавить("multipart.part_headers", "Заголовки части составного тела не будут переданы.");
	Добавить("multipart.filename", "Заданное имя файла части составного тела не сохранится.");
	Добавить("multipart.content_type", "Тип содержимого части составного тела не сохранится.");
	Добавить("data.urlencode", "Значение не будет автоматически закодировано для передачи в адресе.");
	Добавить("input.file", "Содержимое указанного файла не будет передано.");
	Добавить("input.stdin", "Данные из стандартного ввода не будут переданы.");
	Добавить("input.stdin_nonblocking", "Неблокирующее чтение стандартного ввода не поддерживается.");
	Добавить("auth.credentials", "Учётные данные не будут переданы.");
	Добавить("auth.basic", "Обычная проверка подлинности не будет настроена.");
	Добавить("auth.digest", "Дайджест-проверка подлинности не будет настроена.");
	Добавить("auth.negotiate", "Согласование способа проверки подлинности не будет настроено.");
	Добавить("auth.ntlm", "Проверка подлинности NTLM не будет настроена.");
	Добавить("auth.oauth2_bearer", "Токен доступа OAuth 2.0 не будет передан.");
	Добавить("auth.selection", "Выбор способа проверки подлинности не сохранится.");
	Добавить("auth.oauth2", "Проверка подлинности OAuth 2.0 не будет настроена.");
	Добавить("auth.aws_sigv4", "Подпись запроса AWS Signature Version 4 не будет сформирована.");
	Добавить("proxy.http", "Генератор не поддерживает прокси-сервер HTTP.");
	Добавить("proxy.https", "Генератор не поддерживает прокси-сервер HTTPS.");
	Добавить("proxy.socks4", "Генератор не поддерживает прокси-сервер SOCKS4.");
	Добавить("proxy.socks4a", "Генератор не поддерживает прокси-сервер SOCKS4A.");
	Добавить("proxy.socks5", "Генератор не поддерживает прокси-сервер SOCKS5.");
	Добавить("proxy.socks5_hostname", "Генератор не поддерживает разрешение имени узла через SOCKS5.");
	Добавить("proxy.credentials", "Учётные данные прокси-сервера не будут переданы.");
	Добавить("proxy.basic", "Обычная проверка подлинности на прокси-сервере не будет настроена.");
	Добавить("proxy.ntlm", "Проверка подлинности NTLM на прокси-сервере не будет настроена.");
	Добавить("proxy.unix_socket", "Подключение через сокет Unix не будет настроено.");
	Добавить("tls.client_certificate", "Клиентский сертификат не будет использован.");
	Добавить("tls.ca_file", "Указанный файл удостоверяющего центра не будет использован.");
	Добавить("tls.system_store", "Системное хранилище сертификатов не будет использовано.");
	Добавить("timeout.total", "Общее ограничение времени выполнения не будет применено.");
	Добавить("timeout.connect", "Ограничение времени соединения не будет применено.");
	Добавить("ftp.active", "Активный режим FTP не будет включён.");
	Добавить("ftp.passive", "Пассивный режим FTP не будет включён.");
	Добавить("redirect.follow", "Переход по перенаправлениям не будет включён.");
	Добавить("retry.count", "Повторные попытки запроса не будут выполнены.");
	Добавить("retry.max_time", "Ограничение времени повторных попыток не будет применено.");
	Добавить("output.file", "Ответ не будет записан в указанный файл.");
	Добавить("output.remote_name", "Имя файла ответа не будет получено из адреса.");
	Добавить("output.directory", "Ответ не будет записан в указанный каталог.");
	Добавить("output.create_directories", "Каталоги для файла ответа не будут созданы.");
	Добавить("values.expressions", "Генератор не поддерживает значения с вычисляемыми фрагментами.");
	Добавить("values.deferred_classification", "Назначение значения можно определить только после его вычисления.");

КонецПроцедуры

Процедура Добавить(Идентификатор, Последствие)

	_Последствия.Вставить(Идентификатор, Последствие);

КонецПроцедуры

#КонецОбласти
