Перем _Вид; // Строка
Перем _Источник; // ЭлементРазобраннойКомандыCURL

#Область ОбработчикиСобытий

// Создаёт способ аутентификации curl.
//
// Параметры:
//   Вид - Строка - Значение из модуля СпособыАутентификацииCURL.
//   Источник - ЭлементРазобраннойКомандыCURL - Исходная опция.
Процедура ПриСозданииОбъекта(Вид, Источник)
	_Вид = Вид;
	_Источник = Источник;
КонецПроцедуры

#КонецОбласти

#Область ПрограммныйИнтерфейс

// Возвращает вид способа аутентификации.
//
// Возвращаемое значение:
//   Строка - Значение из модуля СпособыАутентификацииCURL.
Функция Вид() Экспорт
	Возврат _Вид;
КонецФункции

// Возвращает исходную опцию.
//
// Возвращаемое значение:
//   ЭлементРазобраннойКомандыCURL - Исходная опция.
Функция Источник() Экспорт
	Возврат _Источник;
КонецФункции

#КонецОбласти
