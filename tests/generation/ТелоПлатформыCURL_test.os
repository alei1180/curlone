#Использовать errors
#Использовать "../../src/syntax"
#Использовать "../../src/domain"
#Использовать "../../src/public"
#Использовать "../../src/generators/platform-http"
#Использовать "../../src/generators/shared"
#Использовать "../domain"

#Область Тесты

&ПараметризованныйТест
&ИсточникЗначение("-d", Истина)
&ИсточникЗначение("--data-binary", Ложь)
Процедура ТестДолжен_СохранятьРежимФайлаПриПереносеВСтрокуЗапроса(Опция, УдалятьПереводыСтрок) Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды("curl -G " + Опция + " @missing.txt https://example.org");
	Строки = Новый ПостроительКодаCURL;
	Генератор = СоздатьФормировательТела();

	// Действие
	Генератор.ДобавитьСтрокуЗапроса(
		Строки,
		Запрос.СтрокаЗапроса().Части(),
		КодыВозможностейГенератораCURL.Все()
	);
	Код = Строки.ПолучитьРезультат();

	// Проверка
	Ожидаем.Что(Код).Содержит("Новый ДвоичныеДанные(""missing.txt"")");
	Ожидаем.Что(СтрНайти(Код, "{t(ТребуетсяУдалениеБайтовДанных)}") > 0)
		.Равно(УдалятьПереводыСтрок);

КонецПроцедуры

&ПараметризованныйТест
&ИсточникЗначение("-d value", "body.data")
&ИсточникЗначение("-T missing.bin", "body.upload")
&ИсточникЗначение("-F field=value", "body.multipart")
&ИсточникЗначение("--data-urlencode name=value", "data.urlencode")
Процедура ТестДолжен_ПропускатьНеподдержанноеТело(Опции, ИсключеннаяВозможность) Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды("curl " + Опции + " https://example.org");
	Строки = Новый ПостроительКодаCURL;
	Возможности = ВозможностиКроме(ИсключеннаяВозможность);
	Генератор = СоздатьФормировательТела();

	// Действие
	Генератор.ДобавитьТелоИзЧастей(Строки, Запрос.Тело().Части(), Возможности);

	// Проверка
	Ожидаем.Что(Строки.ПолучитьРезультат()).Равно("");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ПропускатьНеподдержаннуюСтрокуЗапроса() Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды("curl --url-query name=value https://example.org");
	Строки = Новый ПостроительКодаCURL;
	Генератор = СоздатьФормировательТела();

	// Действие
	Генератор.ДобавитьСтрокуЗапроса(Строки, Запрос.СтрокаЗапроса().Части(), ВозможностиКроме("query.parts"));

	// Проверка
	Ожидаем.Что(Строки.ПолучитьРезультат()).Равно("");

КонецПроцедуры

&ПараметризованныйТест
&ИсточникЗначение("multipart.filename", "filename=")
&ИсточникЗначение("multipart.content_type", "Content-Type: text/custom")
&ИсточникЗначение("multipart.part_headers", "X-Part: value")
Процедура ТестДолжен_ПропускатьНеподдержанныеСвойстваЧасти(Возможность, Фрагмент) Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды(
		"curl -F 'field=@missing.bin;filename=sent.bin;type=text/custom;headers=X-Part: value' https://example.org"
	);
	Строки = Новый ПостроительКодаCURL;
	Генератор = СоздатьФормировательТела();

	// Действие
	Генератор.ДобавитьТелоИзЧастей(Строки, Запрос.Тело().Части(), ВозможностиКроме(Возможность));
	Код = Строки.ПолучитьРезультат();

	// Проверка
	Ожидаем.Что(СтрНайти(Код, Фрагмент)).Равно(0);
	Ожидаем.Что(Код).Содержит("Новый ДвоичныеДанные(""missing.bin"")");

КонецПроцедуры

&Тест
Процедура ТестДолжен_УважатьЗаданныйТипСодержимого() Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды("curl -d value https://example.org");
	Строки = Новый ПостроительКодаCURL;
	Генератор = СоздатьФормировательТела();

	// Действие
	Генератор.ДобавитьТелоИзЧастей(
		Строки,
		Запрос.Тело().Части(),
		КодыВозможностейГенератораCURL.Все(),
		Истина
	);

	// Проверка
	Ожидаем.Что(СтрНайти(Строки.ПолучитьРезультат(), "Content-Type")).Равно(0);

КонецПроцедуры

&Тест
Процедура ТестДолжен_СоединятьБайтыФайлаИТекста() Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды("curl --data-binary @missing.bin -d second https://example.org");
	Строки = Новый ПостроительКодаCURL;
	Генератор = СоздатьФормировательТела();

	// Действие
	Генератор.ДобавитьТелоИзЧастей(Строки, Запрос.Тело().Части(), КодыВозможностейГенератораCURL.Все());
	Код = Строки.ПолучитьРезультат();

	// Проверка
	Ожидаем.Что(Код).Содержит("Новый ДвоичныеДанные(""missing.bin"")");
	Ожидаем.Что(Код).Содержит("СоединитьДвоичныеДанные");
	Ожидаем.Что(Код).Содержит("УстановитьТелоИзДвоичныхДанных");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ИсключатьФайлыБезВозможностиЧтения() Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды("curl --data-binary @missing.bin https://example.org");
	Строки = Новый ПостроительКодаCURL;
	Генератор = СоздатьФормировательТела();

	// Действие
	Генератор.ДобавитьТелоИзЧастей(Строки, Запрос.Тело().Части(), Новый Массив);

	// Проверка
	Ожидаем.Что(Строки.ПолучитьРезультат()).Равно("");

КонецПроцедуры

&Тест
Процедура ТестДолжен_СообщатьОДополнительномКодированииТела() Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды("curl --data-urlencode 'name=a b' https://example.org");
	Строки = Новый ПостроительКодаCURL;
	Генератор = СоздатьФормировательТела();

	// Действие
	Генератор.ДобавитьТелоИзЧастей(Строки, Запрос.Тело().Части(), КодыВозможностейГенератораCURL.Все());
	Код = Строки.ПолучитьРезультат();

	// Проверка
	Ожидаем.Что(Код).Содержит("// {t(ТребуетсяКодированиеДанныхURL)}");
	ПроверитьОтсутствиеРукописнойОбработкиБайтов(Код);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ОформлятьЧастиMultipart() Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды(
		"curl -F 'field=value' -F 'file=@missing.bin;type=application/octet-stream' https://example.org"
	);
	Строки = Новый ПостроительКодаCURL;
	Генератор = СоздатьФормировательТела();

	// Действие
	Генератор.ДобавитьТелоИзЧастей(Строки, Запрос.Тело().Части(), КодыВозможностейГенератораCURL.Все());
	Код = Строки.ПолучитьРезультат();

	// Проверка
	Ожидаем.Что(Код).Содержит("multipart/form-data; boundary=");
	Ожидаем.Что(Код).Содержит("Content-Disposition: form-data; name=");
	Ожидаем.Что(Код).Содержит("Content-Type: application/octet-stream");
	Ожидаем.Что(Код).Содержит("Новый ДвоичныеДанные(""missing.bin"")");

КонецПроцедуры

&Тест
Процедура ТестДолжен_СообщатьОДополнительномКодированииСтрокиЗапроса() Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды("curl --url-query 'name=a b' https://example.org");
	Строки = Новый ПостроительКодаCURL;
	Генератор = СоздатьФормировательТела();

	// Действие
	Генератор.ДобавитьСтрокуЗапроса(
		Строки,
		Запрос.СтрокаЗапроса().Части(),
		КодыВозможностейГенератораCURL.Все()
	);
	Код = Строки.ПолучитьРезультат();

	// Проверка
	Ожидаем.Что(Код).Содержит("// {t(ТребуетсяКодированиеДанныхURL)}");
	Ожидаем.Что(Код).Содержит("""name="" + ТекстЧасти");
	ПроверитьОтсутствиеРукописнойОбработкиБайтов(Код);

КонецПроцедуры

&Тест
Процедура ТестДолжен_ОставитьКомментарийДляИтоговойЛокализации() Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды("curl --url-query 'name=a b' https://example.org");
	Строки = Новый ПостроительКодаCURL;
	Генератор = СоздатьФормировательТела();
	ОжидаемыйКод = "ДанныеЧасти = ПолучитьДвоичныеДанныеИзСтроки(""a b"", КодировкаТекста.UTF8, Ложь);
	|// {t(ТребуетсяКодированиеДанныхURL)}
	|ТекстЧасти = ПолучитьСтрокуИзДвоичныхДанных(ДанныеЧасти, КодировкаТекста.UTF8);
	|Адрес = Адрес + ?(СтрНайти(Адрес, ""?"") = 0, ""?"", ""&"") + ""name="" + ТекстЧасти;";

	// Действие
	Генератор.ДобавитьСтрокуЗапроса(
		Строки,
		Запрос.СтрокаЗапроса().Части(),
		КодыВозможностейГенератораCURL.Все()
	);

	// Проверка
	Ожидаем.Что(Строки.ПолучитьРезультат()).Равно(ОжидаемыйКод);

КонецПроцедуры

&Тест
Процедура ТестДолжен_СообщатьОДополнительномУдаленииБайтовИзФайла() Экспорт

	// Подготовка
	Запрос = ЗапросИзКоманды("curl -d @missing.txt https://example.org");
	Строки = Новый ПостроительКодаCURL;
	Генератор = СоздатьФормировательТела();

	// Действие
	Генератор.ДобавитьТелоИзЧастей(Строки, Запрос.Тело().Части(), КодыВозможностейГенератораCURL.Все());
	Код = Строки.ПолучитьРезультат();

	// Проверка
	Ожидаем.Что(Код).Содержит("// {t(ТребуетсяУдалениеБайтовДанных)}");
	ПроверитьОтсутствиеРукописнойОбработкиБайтов(Код);

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ВозможностиКроме(ИсключеннаяВозможность)

	Результат = Новый Массив;
	Для Каждого Возможность Из КодыВозможностейГенератораCURL.Все() Цикл
		Если Возможность <> ИсключеннаяВозможность Тогда
			Результат.Добавить(Возможность);
		КонецЕсли;
	КонецЦикла;

	Возврат Результат;

КонецФункции

Функция ЗапросИзКоманды(Текст)

	Команда = Новый КомандыДляТестовCURL().ИзСтроки(Текст);
	Интерпретация = Новый ИнтерпретаторКомандыCURL().Интерпретировать(Команда);
	Возврат Интерпретация.Выполнение().Операции()[0].Запрос();

КонецФункции

Функция СоздатьФормировательТела()

	Выражения = Новый ГенераторВыраженийCURL(
		ЛокалиКонвертацииCURL.Русская(),
		Новый Соответствие
	);

	Возврат Новый ФормированиеТелаHTTPЗапросаCURL(Выражения);

КонецФункции

Процедура ПроверитьОтсутствиеРукописнойОбработкиБайтов(Код)

	Ожидаем.Что(СтрНайти(Код, "БуферЧасти")).Равно(0);
	Ожидаем.Что(СтрНайти(Код, "ШестнадцатеричныеДанные")).Равно(0);
	Ожидаем.Что(СтрНайти(Код, "ОчищенныеДанные")).Равно(0);
	Ожидаем.Что(СтрНайти(Код, "ЦифрыHEX")).Равно(0);

КонецПроцедуры
#КонецОбласти
