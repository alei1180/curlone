#Использовать errors
#Использовать "../../src/public"
#Использовать "../../src/generators/platform-http"

#Область Тесты

&Тест
Процедура ТестДолжен_ЗаявлятьСовместимыеВычисляемыеЗначенияБезСтандартногоВвода() Экспорт

	// Подготовка
	Возможности = Новый ГенераторКодаПлатформыCURL().Возможности();

	// Действие и Проверка
	Ожидаем.Что(Возможности.Найти("values.expressions")).Не_().Равно(Неопределено);
	Ожидаем.Что(Возможности.Найти("values.deferred_classification")).Не_().Равно(Неопределено);
	Ожидаем.Что(Возможности.Найти("input.stdin")).Равно(Неопределено);
	Ожидаем.Что(Возможности.Найти("retry.count")).Равно(Неопределено);

КонецПроцедуры

#КонецОбласти
